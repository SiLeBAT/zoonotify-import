# Base URL of the Zoonotify CMS (no trailing slash). HTTPS in production.
STRAPI_URL=http://localhost:3000

# API token tied to the dedicated "Import" Strapi role (see zoonotify-cms README).
STRAPI_TOKEN=replace-with-import-role-token
